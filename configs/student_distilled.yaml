# Compact student distilled from the teacher ensemble
widths: [40, 80, 160]
depths: [2, 2, 2]
epochs: 300
aug_tier: light          # distillation runs disable mixing internally
teacher_logits: results/teacher_logits.npz
kd_temperature: 4.0
kd_alpha: 0.7
out_dir: results/student_distilled
